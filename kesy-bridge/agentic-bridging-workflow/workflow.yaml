# ==========================================================================
# CRE WORKFLOW SETTINGS FILE — Agentic Bridging
# ==========================================================================
# HTTP-triggered workflow for bridge simulation + AI analysis

# ==========================================================================
staging-settings:
  user-workflow:
    workflow-name: "agentic-bridging-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

tenderly-settings:
  user-workflow:
    workflow-name: "agentic-bridging-tenderly"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.tenderly.json"
    secrets-path: "../secrets.yaml"
